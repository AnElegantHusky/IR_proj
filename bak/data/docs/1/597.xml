<?xml version='1.0' encoding='utf-8'?>
<doc><id>597</id><url>https://docs.python.org/zh-cn/3/c-api/code.html</url><title>代码对象</title><body>代码对象是 CPython 实现的低级细节。 每个代表一块尚未绑定到函数中的可执行代码。
用于描述代码对象的对象的 C 结构。 此类型字段可随时更改。
这是一个 PyTypeObject 实例，其表示Python的 code 类型。
如果 co 是一个 code 对象则返回 true。
返回 co 中的自由变量数。
返回一个新的代码对象。 如果你需要一个虚拟代码对象来创建一个代码帧，请使用 PyCode_NewEmpty()。 调用 PyCode_New() 直接可以绑定到准确的 Python 版本，因为字节码的定义经常变化。
类似于 PyCode_New()，但带有一个额外的 "posonlyargcount" 用于仅限位置参数。
3.8 新版功能.
返回具有指定文件名、函数名和第一行号的新空代码对象。 对于 exec() 或 eval() 生成的代码对象是非法的。
Cell 对象
文件对象
</body></doc>